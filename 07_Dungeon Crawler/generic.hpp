#ifndef GUARD_generic_hpp
#define GUARD_generic_hpp

#include <string>

bool simpleChoice(const std::string&, const std::string&);
int randomNumber();
int randomNumber(const int&, const int&);
int factorial(const int&);

#endif